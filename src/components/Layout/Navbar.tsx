import { Box } from "@mui/material";

function Navbar() {
  return (
    <Box
      style={{
        width: "100%",
        height: "60px",
        backgroundColor: "#000",
      }}
    >
      navbar
    </Box>
  );
}

export default Navbar;
